status: disable
settings:
  scope: org
  organizations:
    apidev-apigeex-sandbox-d29a:
      environments:
        dev:
          data:
            name: SAP
            host: alticept.local
            port: 7777
            isEnabled: true
            sSLInfo:
              enabled: true
            protocol: HTTP
