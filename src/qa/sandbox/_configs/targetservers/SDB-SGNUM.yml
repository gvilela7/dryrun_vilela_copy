status: disable
settings:
  scope: org
  organizations:
    apidev-apigeex-sandbox-d29a:
      environments:
        dev:
          data:
            name: SDB-SGNUM
            host: pre-release.services.bk.sapo.pt
            port: 443
            isEnabled: true
            sSLInfo:
              enabled: true
            protocol: HTTP
