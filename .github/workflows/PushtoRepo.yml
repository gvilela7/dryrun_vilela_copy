name: Create Branch in Target Repository

on:
  workflow_dispatch:

jobs:
  create-branch:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout source code
      uses: actions/checkout@v2

    - name: Create branch in target repository
      env:
        TARGET_REPO_TOKEN: ${{ secrets.TARGET_REPO_TOKEN }}
        TARGET_REPO_USERNAME: gvilela7
        TARGET_REPO_NAME: dryrun_vilela
      run: |
        # Set up Git
        git config --global user.email "actions@github.com"
        git config --global user.name "GitHub Actions"

        # Create a new branch locally
        git checkout -b new_branch_name

        # Push the new branch to the target repository
        git push https://$TARGET_REPO_USERNAME:$TARGET_REPO_TOKEN@github.com/$TARGET_REPO_USERNAME/$TARGET_REPO_NAME.git new_branch_name